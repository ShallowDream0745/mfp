# Multi-modal Flow Planner

## Notes
Our paper reports the results after 1 million (1M) iterations. The default action repeat is 2.


## Quick Start Instructions

```bash
conda create -n mfp python=3.12
conda activate mfp
pip install -r requirements.txt
pip install -e .
cd mfp
python train.py 
```